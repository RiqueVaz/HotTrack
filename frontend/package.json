{
  "name": "hottrack-frontend",
  "version": "2.0.0",
  "description": "Frontend do HotTrack SaaS",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -s . -p 3001",
    "dev": "npx live-server --port=3001 --host=localhost",
    "build": "echo 'Static files - no build needed'",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "frontend",
    "hottrack",
    "saas"
  ],
  "author": "HotTrack Team",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
